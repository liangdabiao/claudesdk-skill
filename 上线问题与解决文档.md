# *-Chat 项目 VPS + 宝塔上线问题与解决文档

## 环境

- VPS: 阿里云 Linux
- 面板: 宝塔
- 进程管理: PM2
- 反向代理: Nginx (宝塔内置)
- Node.js: v20+ (必须)
- Python3 + pip: requests, openpyxl

---

## 问题 1: Node.js 版本过低，构建失败

**报错:**
```
npm warn EBADENGINE   required: { node: '>= 20' },
npm warn EBADENGINE   current: { node: 'v18.20.8' }
failed to load config from vite.config.ts
```

**原因:** Tailwind CSS v4 / Vite 6 要求 Node.js >= 20，宝塔默认安装的是 v18。

**解决:**
```bash
nvm install 20
nvm use 20
node -v  # 确认 v20.x
```

---

## 问题 2: 权限不足，目录创建失败

**报错:**
```
Error: EACCES: permission denied, mkdir '/www/wwwroot/exa-chat/dist/server/logs'
Error: EACCES: permission denied, mkdir '/www/wwwroot/exa-chat/dist/server/uploads'
```

**原因:** 编译后的服务代码 `__dirname` 指向 `dist/server/`，在该目录下创建 logs/uploads/reports 等子目录。PM2 以 `www` 用户运行，项目目录属于 root，没有写权限。

**解决:**
```bash
# 手动创建所需目录
mkdir -p /www/wwwroot/exa-chat/dist/server/uploads
mkdir -p /www/wwwroot/exa-chat/dist/server/reports
mkdir -p /www/wwwroot/exa-chat/dist/server/review-analysis-reports
mkdir -p /www/wwwroot/exa-chat/dist/server/logs

# 修改整个项目权限
chown -R www:www /www/wwwroot/exa-chat
```

---

## 问题 3: 宝塔创建的 PM2 进程加载不到 .env

**现象:**
```
injected env (0) from .env  # 0 个环境变量
```
进程无限重启（PM2 restart 次数不断增长）。

**原因:** 宝塔面板创建的 PM2 进程工作目录和配置与手动启动的不同，导致 `dotenv` 找不到 `.env` 文件。

**解决:**
```bash
# 删除宝塔创建的进程
pm2 delete exa-chat

# 手动启动，确保工作目录正确
cd /www/wwwroot/exa-chat
pm2 start dist/server/index.js --name exa-chat --uid www --gid www
pm2 save
```

启动后应看到 `injected env (7)` 表示环境变量加载成功。

---

## 问题 4: 静态文件路径重复 `dist/dist/index.html`

**报错:**
```
Error: ENOENT: no such file or directory, stat '/www/wwwroot/exa-chat/dist/dist/index.html'
```

**原因:** 编译后的 `server/index.js` 位于 `dist/server/`，`__dirname` = `dist/server/`。源码中写的是 `path.join(__dirname, "..", "dist")`，拼接后变成 `dist/server/../dist` = `dist/dist/`，路径重复。

**解决:** 修改 `server/index.ts`：
```typescript
// 修改前
const distPath = path.join(__dirname, "..", "dist");

// 修改后
const distPath = path.join(__dirname, "..");
```

因为 `__dirname`（`dist/server/`）的上一级已经是 `dist/`。

> **注意:** `financial-chart-chat` 已用 `fs.existsSync` 检测处理了此问题，不需要修改。

---

## 问题 5: HTTP 下 `crypto.randomUUID()` 不可用

**报错:**
```
Uncaught TypeError: crypto.randomUUID is not a function
```

**原因:** `crypto.randomUUID()` 要求安全上下文（HTTPS），通过 HTTP 访问时不可用。

**解决:** 修改 `src/hooks/useWebSocket.ts`：

```typescript
// 在文件顶部添加兼容函数
const uuid = () => crypto.randomUUID?.() ?? `${Date.now()}-${Math.random().toString(36).slice(2)}`;

// 替换所有 crypto.randomUUID() 为 uuid()
```

> **涉及项目:** 所有使用 `crypto.randomUUID()` 的项目都需修改。`amazon-chat`、`exa-chat`、`financial-chart-chat` 已修复。

---

## 问题 6: Claude CLI 未安装或找不到

**报错:**
```
Error: ProcessTransport is not ready for writing
    at ProcessTransport.write (.../@anthropic-ai/claude-agent-sdk/sdk.mjs:7861:13)
```

**原因:** `claude-agent-sdk` 依赖 Claude CLI (`@anthropic-ai/claude-code`)。注意：SDK 不是调用全局 `claude` 命令，而是用自带的 `cli.js`（位于 `node_modules/@anthropic-ai/claude-agent-sdk/cli.js`），但 CLI 仍需全局安装才能正常初始化。

**解决:**
```bash
npm install -g @anthropic-ai/claude-code
claude --version  # 确认安装成功
```

如果 PM2 找不到 `claude` 命令（宝塔安装的 Node.js 路径特殊），创建软链接：
```bash
# 找到 claude 的实际路径
which claude  # 例如 /www/server/nodejs/v24.16.0/bin/claude

# 创建软链接到系统 PATH
ln -s /www/server/nodejs/v24.16.0/bin/claude /usr/local/bin/claude
```

---

## 问题 7: Claude CLI 未登录

**报错:**
```
Not logged in · Please run /login
```
或 SDK 日志中 `ProcessTransport is not ready for writing`。

**原因:** Claude CLI 需要登录才能使用。即使用 `.env` 中配置了 `ANTHROPIC_API_KEY`，CLI 仍可能要求登录。VPS 上首次使用时未登录。

**调试方法:** 开启 SDK 调试日志：
```bash
DEBUG_CLAUDE_AGENT_SDK=1 node dist/server/index.js
# 日志输出到: /root/.claude/debug/sdk-*.txt
```

**解决:**
```bash
# 方法 1：在 VPS 上直接登录（需要浏览器交互）
claude login

# 方法 2：从本地复制登录凭据
# 本地 Windows 上找到凭据目录：
#   %USERPROFILE%\.claude\
# 上传到 VPS 的 /root/.claude/ 目录
```


# 停止所有 Claude Code 进程
pkill -f claude

# 设置正确的环境变量
export ANTHROPIC_BASE_URL="https://api.deepseek.com/anthropic"
export ANTHROPIC_AUTH_TOKEN="sk-你的DeepSeek密钥"  # 注意：是 AUTH_TOKEN 不是 API_KEY
export ANTHROPIC_MODEL="deepseek-v4-pro[1m]"
export ANTHROPIC_DEFAULT_OPUS_MODEL="deepseek-v4-pro[1m]"
export ANTHROPIC_DEFAULT_SONNET_MODEL="deepseek-v4-pro[1m]"
export ANTHROPIC_DEFAULT_HAIKU_MODEL="deepseek-v4-flash"
export CLAUDE_CODE_SUBAGENT_MODEL="deepseek-v4-flash"
export CLAUDE_CODE_EFFORT_LEVEL="max"
export API_TIMEOUT_MS="600000"

# 永久保存到 ~/.bashrc
cat >> ~/.bashrc << 'EOF'
export ANTHROPIC_BASE_URL="https://api.deepseek.com/anthropic"
export ANTHROPIC_AUTH_TOKEN="sk-你的DeepSeek密钥"
export ANTHROPIC_MODEL="deepseek-v4-pro[1m]"
export ANTHROPIC_DEFAULT_OPUS_MODEL="deepseek-v4-pro[1m]"
export ANTHROPIC_DEFAULT_SONNET_MODEL="deepseek-v4-pro[1m]"
export ANTHROPIC_DEFAULT_HAIKU_MODEL="deepseek-v4-flash"
export CLAUDE_CODE_SUBAGENT_MODEL="deepseek-v4-flash"
export CLAUDE_CODE_EFFORT_LEVEL="max"
export API_TIMEOUT_MS="600000"
EOF

source ~/.bashrc


---

## 问题 8: root 用户下 bypassPermissions 被拒绝（核心问题）

**报错:**
```
SDK debug log 中最后一条:
--dangerously-skip-permissions cannot be used with root/sudo privileges for security reasons
```

**现象:** 服务以 root 身份运行时，`node dist/server/index.js` 立即 `Terminated`，没有任何其他错误。只有开启 `DEBUG_CLAUDE_AGENT_SDK=1` 才能看到这条原因。

**原因:** `claude-agent-sdk` 使用 `permissionMode: "bypassPermissions"`，对应 CLI 参数 `--dangerously-skip-permissions`。Claude CLI 出于安全考虑，**拒绝在 root/sudo 用户下使用此模式**。

**解决:** 必须以非 root 用户（如 `www`）运行 PM2 进程：

```bash
# 1. 把 root 的 claude 登录凭据复制给 www 用户
mkdir -p /home/www/.claude
cp -r /root/.claude/* /home/www/.claude/
chown -R www:www /home/www/.claude

# 2. 确保项目目录权限正确
chown -R www:www /www/wwwroot/exa-chat

# 3. 以 www 用户启动 PM2
pm2 delete exa-chat 2>/dev/null
pm2 start dist/server/index.js --name exa-chat --uid www --gid www
pm2 save
```

> **关键点：** 这一步是解决 `ProcessTransport is not ready for writing` 的最终方案。前面安装 Claude CLI、登录、创建软链接都是前置条件，但如果以 root 运行，所有这些都不够，必须在 www 用户下运行。

---

## 问题 9: Nginx 400 Bad Request (Header Or Cookie Too Large)

**报错:**
```
400 Bad Request
Request Header Or Cookie Too Large
```

**解决:** 在 Nginx 站点配置的 `server {}` 块中添加：
```nginx
large_client_header_buffers 4 32k;
client_header_buffer_size 32k;
```

---

## 问题 10: Docker 残留占用 C 盘空间（本地 Windows）

**现象:** 删除 Docker 镜像后 C 盘空间未释放。

**原因:** WSL2 虚拟磁盘 (`ext4.vhdx`) 不会自动缩小。

**解决:**
```bash
# 清理 Docker 缓存
docker system prune -a
docker builder prune -a

# 压缩虚拟磁盘（先退出 Docker Desktop）
wsl --shutdown
diskpart
# 在 diskpart 中：
select vdisk file="C:\Users\你的用户名\AppData\Local\Docker\wsl\data\ext4.vhdx"
compact vdisk
exit
```

如果不使用 Docker，直接卸载 Docker Desktop 并删除：
```
C:\Users\用户名\AppData\Local\Docker
C:\Users\用户名\AppData\Roaming\Docker
C:\Program Files\Docker
```

---

## 标准上线流程（快速参考）

```bash
# 1. 确认 Node.js 版本
node -v  # 需要 v20+

# 2. 配置 .env
cd /www/wwwroot/exa-chat
cat > .env << 'EOF'
ANTHROPIC_API_KEY=sk-xxx
ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
MODEL=deepseek-v4-flash
TIKHUB_TOKEN=xxx
PORT=3005
EOF

# 3. 生成 .mcp.json（如项目需要）
cat > .mcp.json << 'EOF'
{ "mcpServers": { "sorftime": { "url": "https://mcp.sorftime.com?key=xxx" } } }
EOF

# 4. 安装依赖 + 构建
npm install
npm run build

# 5. 创建运行时目录 + 赋权
mkdir -p dist/server/{uploads,reports,review-analysis-reports,logs}
chown -R www:www .

# 6. 安装 Claude CLI + 登录
npm install -g @anthropic-ai/claude-code
claude login  # 或从本地复制 ~/.claude/ 凭据

# 7. 将 claude 凭据复制给 www 用户
mkdir -p /home/www/.claude
cp -r /root/.claude/* /home/www/.claude/
chown -R www:www /home/www/.claude

# 8. 创建 claude 软链接（宝塔环境需要）
ln -s $(which claude) /usr/local/bin/claude

# 9. 启动 PM2（必须以 www 用户运行，不能用 root）
pm2 delete exa-chat 2>/dev/null
pm2 start dist/server/index.js --name exa-chat --uid www --gid www
pm2 save

# 10. 验证
sleep 3
pm2 logs exa-chat --lines 10   # 确认 injected env (7)
netstat -tlnp | grep node      # 确认端口监听
curl -I http://127.0.0.1:3005/  # 确认 HTTP 200
```

---

## Nginx 反向代理配置参考

```nginx
location / {
    proxy_pass http://127.0.0.1:3005;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    large_client_header_buffers 4 32k;
    client_header_buffer_size 32k;
}

location /ws {
    proxy_pass http://127.0.0.1:3005;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_read_timeout 86400;
}
```

---

## 调试技巧

### 查看 SDK 调试日志

```bash
# 开启调试模式，日志输出到文件
DEBUG_CLAUDE_AGENT_SDK=1 node dist/server/index.js
# 日志位置: /root/.claude/debug/sdk-*.txt
cat /root/.claude/debug/sdk-*.txt | tail -30
```

### 前台运行测试

```bash
# 停掉 PM2，前台运行看完整错误
pm2 stop exa-chat
node dist/server/index.js
# 或带调试
DEBUG_CLAUDE_AGENT_SDK=1 node dist/server/index.js
```

### 常用排查命令

```bash
pm2 status                          # 查看进程状态（关注 restart 次数）
pm2 logs exa-chat --lines 30        # 查看日志
pm2 flush exa-chat                  # 清空旧日志
netstat -tlnp | grep node           # 查看端口监听
curl -I http://127.0.0.1:3005/      # 测试 HTTP 响应
which claude                        # 确认 claude 路径
cat ~/.claude/debug/sdk-*.txt       # 查看 SDK 调试日志
```

---

## 更新部署

```bash
cd /www/wwwroot/exa-chat
git pull
npm install
npm run build
mkdir -p dist/server/{uploads,reports,review-analysis-reports,logs}
chown -R www:www .
pm2 restart exa-chat
```
